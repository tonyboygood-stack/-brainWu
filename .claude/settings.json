{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git fetch origin && git checkout main && git pull origin main 2>/dev/null || true",
            "statusMessage": "同步 main 分支中..."
          }
        ]
      }
    ]
  }
}
