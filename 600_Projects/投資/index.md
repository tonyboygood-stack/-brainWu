---
title: 投資
type: project
status: 🟠 進行中
created: 2026-09-07
tags:
  - 投資
  - 專案
---

# 投資

## 這個專案在做什麼

把 YouTube 財經頻道的內容批次轉成逐字稿，累積成可搜尋、可建卡的原始素材庫。逐字稿本身是**未整理的原文**，性質等同 `400_Atlas/Library/raw/`——先囤起來，之後要出觀點再走 [[note-capture]] 建卡。

## 素材來源

[宏爺講股](https://www.youtube.com/@宏爺講股)，491 支、169 小時、平均 20.6 分鐘。內容分兩類，逐字稿也依此分開存放：

| 類型 | 數量 | 內容 | 存放位置 |
| :--- | ---: | :--- | :--- |
| **每日分析** | 432 | 時事與盤勢，標題帶日期，公開影片 | `逐字稿/每日分析/` |
| **會員專題** | 59 | 觀念教學與專題分析，會員限定 | `逐字稿/會員專題/` |

會員專題的標題自帶流水號（`[海豚以上學員專屬(85)]`），檔名用 `NNN_日期_標題` 開頭，依期數排序而非日期。目前清單涵蓋第 8～85 期，中間缺約 19 期，待會員權限恢復後再查是否另有未列在 `/videos` 頁籤的影片。

分類方式：優先看 yt-dlp 的 `availability` 欄位（`subscriber_only` 即會員限定），並用標題規則兜底——後者在清單階段就分得出來，不需要會員權限。每份逐字稿的 frontmatter 都有 `category`、`access` 兩個欄位可供篩選。

> [!note] 收錄狀態更新（2026-09-09）
> 目前會員專題資料夾已有59份檔案。早期「全部抓不到」是歷史狀態；不代表所有缺期已補齊，也不保證逐字稿每一數字正確。

## 逐字稿工具

腳本：[[yt-transcribe.py]]（`000_Agent/scripts/yt-transcribe.py`）

兩段式管線，先快後慢：

1. **快車道** — 影片有 YouTube 字幕軌就直接下載解析，一支 2–5 秒。抽樣顯示絕大多數影片走這條。
2. **慢車道** — 沒字幕軌的（會員影片、硬字幕影片、剛上傳還沒生成字幕的）才下載 m4a 音訊，交給 faster-whisper `large-v3-turbo` 轉錄。

### 常用指令

抓整個頻道最新 30 支：

```bash
python 000_Agent/scripts/yt-transcribe.py "https://www.youtube.com/@宏爺講股/videos" --limit 30
```

先只收有現成字幕的（最快，把便宜的先撈乾淨）：

```bash
python 000_Agent/scripts/yt-transcribe.py "https://www.youtube.com/@宏爺講股/videos" --skip-whisper
```

看有哪些待處理但不執行：

```bash
python 000_Agent/scripts/yt-transcribe.py "https://www.youtube.com/@宏爺講股/videos" --dry-run
```

抓單支（含會員影片）：

```bash
python 000_Agent/scripts/yt-transcribe.py "https://www.youtube.com/watch?v=影片ID"
```

### 設計上的幾個決定

- **已經是繁體的字幕軌不做任何轉換**，原文照抄。OpenCC 的 `s2twp` 會把「數據」改成「資料」、「設備」改成「裝置」，那是竄改講者原話而非轉換字體。只有 Whisper 輸出（必為簡體）才用 `s2tw` 轉，並附一組「臺→台」的台灣財經慣用詞修正表。
- **只下載 m4a 音訊軌（format 140）**，不做轉檔，因此整條管線不需要 ffmpeg。
- **音訊轉完即刪**，不佔硬碟。
- **斷點續傳**：完成紀錄存在 repo 外的 `state.json`，中斷後直接重跑會接續，不會重做。
- **憑證與暫存放在 repo 外**（`C:\Users\user\.config\yt-transcribe\`），確保 Obsidian Git 不會把 cookie 推上 GitHub。

### 相依套件

`yt-dlp`、`faster-whisper`、`opencc-python-reimplemented`，外加 **Deno**（yt-dlp 用來解 YouTube 的 JS 挑戰，缺了會抓不到下載格式）。

---

# 宏爺股市分析知識體系

以 59 期會員專題（26.3 萬字）為基礎建構的知識庫，目標是能回答實際的股市判讀問題。

## 從哪裡開始

| 你要做什麼 | 打開哪一份 |
| :--- | :--- |
| **了解全貌** | [[00_總框架]]｜單一入口，六層架構與核心決策鏈 |
| **找特定知識點** | [[知識點索引]]｜40 張卡一覽表，可依層級與觸發問句查找 |
| **問 AI 股市問題** | [[判讀SOP]]｜AI 逐層檢查的作業程序與證據誠實規則 |
| **查外部數據** | [[數據來源清單]]｜依分析層次排列，已實測連線 |
| **搜尋逐字稿原文** | [[術語表]]｜逐字稿有系統性錯字，查詢前必看 |

## 六層架構

| 層 | 記憶問句 | 卡片數 |
| :--- | :--- | ---: |
| [[L0_資金環境]] | 錢從哪來、往哪去？ | 4 |
| [[L1_市場位階]] | 現在在週期的哪一段？ | 4 |
| **[[L2_籌碼意圖]]** ★ | 誰在買、誰在賣？ | **8** |
| [[L3_標的體質]] | 這家公司撐得住嗎？ | 3 |
| [[L4_進出時機]] | 什麼時候動手？ | 10 |
| [[L5_部位與風控]] | 錯了怎麼辦？ | 7 |
| [[貫穿層_心理紀律]] | 我在騙自己嗎？ | 4 |
| [[工具層]] | 用什麼看？ | — |

架構依實際內容權重設計，不是教科書式的三等分。籌碼面在全庫出現 1,107 次，是基本面（286 次）的近四倍——所以 L2 是核心層，基本面只當篩選器。

## 核心決策鏈

他把整套方法濃縮成十個字：**價平、量平，大戶持續加碼**。

均線糾結 ＋ 大戶連續加碼 → 口袋名單 → 利空不跌 → 長紅突破 → 進場（乖離過大則不追）

## 維護

新增或修改知識卡後，重跑索引生成器即可：

```bash
python 000_Agent/scripts/build-index.py
```

## 待辦

- [ ] 補完書籍系列（第 43、45、46、72 期）與 AI 學習歷程（第 58 期）的知識卡
- [ ] 補完產業趨勢群（第 59、63、66、68、69、71、75、77、85 期）的輕量摘要卡
- [ ] 補完第 22、79 期的期權操作細節、第 62 期十倍股、第 81 期除權息
- [ ] 第 36 期外資期貨換算係數「×1.4」存疑，需回原片確認
- [ ] 待知識體系穩定後，把近期每日分析拉進來理解當前市場框架
- [ ] 產生自己的觀點時走 note-capture 存入 `400_Atlas/Notes/`

## 相關

- [[yt-transcribe.py]]｜逐字稿工具
- [[build-index.py]]｜索引生成器

## 股票助手資料品質補強

2026-09-09：已修正占量比日期錯配、快取過期、短期間冒充52週與缺資料誤算通過；新增上櫃法人融資、官方董監／期權、歷季財報現金流與總經資料。

詳見 [[系統補強紀錄_2026-09-09]]、[[資料蒐集SOP]]。這是資料助手，仍有需補足的評估證據，不宣稱完全還原宏爺判斷。
